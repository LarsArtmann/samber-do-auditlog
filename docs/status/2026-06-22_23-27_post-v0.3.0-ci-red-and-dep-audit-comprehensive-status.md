# Status Report — 2026-06-22 23:27

**Project**: `samber-do-auditlog` — Go plugin for samber/do v2 DI container audit logging
**Module**: `github.com/larsartmann/samber-do-auditlog` · **Go**: 1.26.3 (go.mod) / 1.26.4 (devShell) · **Status**: ALPHA
**Latest release**: **v0.3.0** (2026-06-21) · **HEAD**: `fc88b76` (1 commit ahead of `v0.3.0`)
**LOC**: ~3,855 hand-written library (+ ~9,080 tests, excl. generated templ / cmd / example)
**Tests**: 278 test/benchmark/fuzz/example functions · **Race**: clean · **Fuzz targets**: 3

**Headline**: 🔴 **CI IS RED** — 3 consecutive failing runs on `master`. Build/vet/tests all pass locally; **Lint** (2 `unparam`) and **go mod tidy** (drift) jobs fail in CI. Coverage dropped 95.5% → **94.6%** and the gate was silently lowered 95% → 94%.

---

## Context: What Just Happened

Since the last status report (`2026-06-21_07-50`), **v0.2.0 and v0.3.0 were both tagged and shipped** (2026-06-21):

- **v0.2.0** — D2 diagram export, `go-output` adoption for all diagram rendering, Plugin API parity across all 9 export formats.
- **v0.3.0** — tree (`WriteTree`/`WriteHTMLTree`) and table (`WriteTable`, 16+ formats) export, plus CLI `convert` coverage for the new formats.
- **Since v0.3.0**: a single commit `fc88b76` ("centralize test fixtures, extract helpers, and bump go-output to v0.17.1") — which is the commit that **broke CI** (introduced 2 `unparam` lint violations and go.sum drift).

This session: verified dependency freshness (all direct deps on latest), ran the full verification suite, and confirmed CI state via `gh run list`.

### Working tree (uncommitted)

| File | Change | Why |
|------|--------|-----|
| `go.mod` / `go.sum` | go-output `v0.17.1` → **`v0.17.2`** (9 of 12 sub-modules; `escape`/`enum`/`envdetect` have no `v0.17.2` tag upstream) | "libs on latest" — verified correct, tidy-consistent |
| `html_templ.go` | templ import block regrouped (single → grouped `import (...)`) | regenerated output matching the pinned templ version |

This bump is **tidy-consistent** (verified: `go mod tidy` produces no diff) and would fix the CI **mod-tidy** job once committed.

---

## A. FULLY DONE ✓

### A.1 Export Formats (11 — all functional, all tested, all documented)

| Format | Report method | Plugin Write | Plugin Export | CLI `convert` | Example demo |
|--------|--------------|--------------|---------------|---------------|--------------|
| JSON | `WriteJSON` | ✓ | ✓ | ✓ | ✓ |
| NDJSON | `WriteNDJSON` | ✓ | ✓ | ✓ | ✓ |
| CSV | `WriteCSV` | ✓ | ✓ | ✓ | ✗ |
| TSV | `WriteTSV` | ✓ | ✓ | ✓ | ✗ |
| HTML | `WriteHTML` | ✓ | ✓ | ✓ | ✓ |
| Mermaid | `WriteMermaid` | ✓ | ✓ | ✓ | ✓ |
| PlantUML | `WritePlantUML` | ✓ | ✓ | ✓ | ✓ |
| DOT | `WriteDOT` | ✓ | ✓ | ✓ | ✓ |
| D2 | `WriteD2` | ✓ | ✓ | ✓ | ✓ |
| **Tree** (ASCII) | `WriteTree` | ✓ | ✓ | ✓ | — |
| **Table** (16+ sub-formats) | `WriteTable` | ✓ | ✓ | ✓ | — |

### A.2 Local verification — ALL GREEN ✓

| Check | Result |
|-------|--------|
| `go build ./...` | ✓ exit 0 |
| `go vet ./...` | ✓ exit 0 |
| `go generate ./...` | ✓ no drift |
| `go mod tidy` (working tree, v0.17.2) | ✓ no drift |
| `go test -race -count=1 ./...` | ✓ all pass, race-clean |
| Coverage (non-example) | 94.6% — passes the 94% gate |

### A.3 CI jobs that DO pass (4 of 6)

| Job | Status |
|-----|--------|
| **Test** (vet + build + race + coverage) | ✓ |
| **Vulnerability scan** (`govulncheck`) | ✓ |
| **Workflow lint** (`actionlint`) | ✓ |
| **Stale generated code** (`go generate`) | ✓ |

### A.4 Core library (production-ready)

- **Plugin lifecycle**: 6 hooks, stack-based dependency inference, build/shutdown duration, health checks, service-type detection, scope tree, single-mutex concurrency, zero-cost disabled mode, `OnEvent` callback, `MaxEvents` cap.
- **Report model**: `Report`, `ServiceInfo`, `ScopeNode`, `Validate()`, `Index()`, convenience queries (`ServiceByName`, `EventsByType`, `FailedServices`, `UnhealthyServices`, …), `Diff()`, `Filtered()`.
- **Replay engine**: `ReplayEvents` reconstructs a `Report` from a flat event stream (inverse of hook recording).
- **Loader**: `LoadReport` auto-detects JSON vs NDJSON.
- **JSON Schema**: generated by `cmd/genschema`, `go:embed`ded, exposed via `JSONSchema()`.
- **Schema migration**: v0.1.0 → v0.2.0 via `MigrateReport`.
- **CLI** (`cmd/auditlog`): `info` / `convert` / `diff` / `validate` / `stats` / `schema`.
- **Hardening**: 3 fuzz targets (HTML XSS, migration integrity, diagram special-chars), HTML golden-file test, atomic file writes, CSP-hardened HTML.

### A.5 Documentation

- **README** (507 lines): Quick Start, all export formats with sample output, full API reference, security model.
- **CHANGELOG**: through `[0.3.0]`.
- **FEATURES.md**: honest feature inventory with file references.
- **AGENTS.md**: file map, architecture, concurrency model, gotchas.
- **TODO_LIST.md**, **STABILITY.md**, **BENCHMARKS.md**, **docs/DOMAIN_LANGUAGE.md**, reference examples (`docs/examples/`: otel, websocket, prometheus, samber-ro).

---

## B. PARTIALLY DONE ⚠️

### B.1 CI — 4 of 6 jobs green, 2 red 🔴
See section D for the failures. The pipeline runs, most jobs pass, but **Lint** and **go mod tidy** are broken.

### B.2 Coverage — above the gate, below the documented target
**94.6%** actual. The gate was lowered from 95% → 94% (see D.2). Local `go test -cover` shows the dip is concentrated in the newly-added tree/table export paths and the test-fixture refactor, not in core logic. Still above the *current* 94% gate, but a regression from the prior **95.5%**.

### B.3 Dependency freshness
All direct deps verified on latest this session (`a-h/templ` v0.3.1020, `invopop/jsonschema` v0.14.0, `samber/do/v2` v2.0.0, `go-branded-id` v0.3.1, `go-output` v0.17.2). The go-output `v0.17.1` → `v0.17.2` bump is **ready in the working tree but uncommitted**. Three sub-modules (`escape`, `enum`, `envdetect`) remain at `v0.17.1` because **no `v0.17.2` tag exists for them upstream** — this is correct, not drift, though the go.mod "tagged together at the same version" comment now reads as slightly misleading.

### B.4 go-output diagram theming (carried over)
Warm-amber palette is preserved **per node** (`GraphStyle{Fill, Stroke, FontColor}`). Two visual elements are still missing, blocked on upstream go-output:
- DOT dark canvas (`bgcolor="#14110d"`) — gone.
- Edge line-colors (Mermaid `lineColor`, PlantUML arrow color) — gone.

### B.5 Property-style testing
`Diff` and `MigrateReport` have property-*style* tests (idempotency, anti-symmetry, corrupt-repair), but no formal property-based framework (`rapid` / `gopter`) is wired up.

---

## C. NOT STARTED

- **Typed identifiers** (`ContainerID` / `ScopeID` / `ServiceName` as named string types) — deferred to a future breaking release.
- **`ServiceInfo` lifecycle split** (identity / lifecycle / health / graph) — deferred alongside typed identifiers.
- **Upstream go-output PRs**: (1) `DedupEdges` for the D2 renderer; (2) graph-attribute setter (for `bgcolor` + edge colors). Neither filed.
- **Restore DOT dark background** — blocked on the upstream graph-attribute setter above.
- **CSV/TSV demo in `example/`** — the delimited exports are tested but not exercised by the self-checking demo.
- **`ROADMAP.md`** — does not exist (long-term direction lives scattered across TODO_LIST / status reports).
- **govulncheck in the pre-commit hook** — currently CI-only.
- **Streaming HTML export** via `go-output/markup` — available upstream, not adopted.
- **Node.js 20 → 24 action bumps** (`actions/checkout@v4`, `actions/setup-go@v5`) — deprecation warning in CI, not yet failing.

---

## D. TOTALLY FUCKED UP! 💥

### D.1 🔴 CI is RED — 3 consecutive failures (the "nobody noticed" pattern, again)
The last 3 runs on `master` all failed (`fc88b76`, `bb98c97`, `a2325b8`). The previous status report (`2026-06-21_07-50`) **explicitly flagged this exact anti-pattern** ("Status reports falsely claimed CI ALL GREEN … broken for ~12 days without anyone noticing") — and it has **recurred**. CI has now been red for ~1+ day.

Latest run (`27962578226`) job breakdown:
- ❌ **Lint** — `golangci-lint` exits 1 on **2 `unparam` issues**: `mkInvAfter` / `mkInvBefore` in `replay_test.go:79,86` — the `containerID` parameter is always passed the literal `"c"`. Introduced by commit `fc88b76` ("centralize test fixtures, extract helpers"). **Fix is trivial** (drop the param or vary the callers), but was not caught before push.
- ❌ **go mod tidy** — drift. The committed `fc88b76` go.sum (go-output v0.17.1) is not tidy-consistent. The uncommitted working-tree bump to v0.17.2 **is** tidy-consistent and would fix this job once committed.

### D.2 The coverage gate was silently lowered 95% → 94%
`scripts/coverage-gate.sh` and `.github/workflows/ci.yml` both enforce **94%**, but `AGENTS.md` still documents the gate as **≥95%** (and the CI coverage-gate description). Actual coverage is **94.6%**, down from the prior **95.5%**. The bar was lowered to match the dip rather than writing the missing tests to restore 95%+. This is a documentation drift **and** a quality-bar regression.

### D.3 `CODE_OF_CONDUCT.md` is missing from the repo
The file does not exist on disk and is **not tracked** by git (`git ls-files CODE_OF_CONDUCT.md` returns nothing). The git history shows repeated `fix: restore CODE_OF_CONDUCT.md deleted by BuildFlow pre-commit hook` / `chore: remove CODE_OF_CONDUCT.md` churn — the **BuildFlow pre-commit hook auto-deletes it** on every normal commit, and the only way to keep it is `--no-verify`. This is unresolved and actively losing the file.

### D.4 TODO_LIST versioning mislabel
`TODO_LIST.md` defers the breaking changes (typed identifiers, `ServiceInfo` split) to **"v0.3.0"**, but **v0.3.0 shipped as a non-breaking** tree/table release. The next-breaking-release target is therefore wrong/misleading. Combined with the missing `ROADMAP.md`, the project's versioning strategy is internally inconsistent (see G).

### D.5 BuildFlow pre-commit hook is destructive and unfixed
The **active** pre-commit hook is BuildFlow (`.git/hooks/pre-commit` → `buildflow --build-mode pre-commit --staged-only`), **not** the repo's own `scripts/hooks/pre-commit` (which runs generate-drift + vet + lint + test — that one is **not installed**, `core.hooksPath` is unset). BuildFlow's documented destructive behaviors — **strips `go.sum` changes** and **deletes `CODE_OF_CONDUCT.md`** — are still live, forcing `--no-verify` churn on every dependency/CoC commit. This is a known, documented, unfixed hazard.

---

## E. WHAT WE SHOULD IMPROVE

### CI / DevOps
1. **Fix CI immediately** — the 2 `unparam` issues + commit the v0.17.2 bump. This is a 15-minute job and unblocks everything.
2. **Stop lowering the bar** — restore the coverage gate to 95% by writing the missing tests on the tree/table paths, rather than documenting 95% while enforcing 94%.
3. **Resolve the BuildFlow hook** — either reconfigure it to stop stripping `go.sum` / deleting CoC, or install the repo's own `scripts/hooks/pre-commit` (`git config core.hooksPath scripts/hooks`) and drop BuildFlow. The current state silently bypasses the intended pre-commit checks.
4. **Restore `CODE_OF_CONDUCT.md`** permanently.
5. **Bump GitHub Actions** off Node.js 20 (`checkout@v4`, `setup-go@v5`) before the deprecation becomes a failure.

### Architecture / Code Quality
6. **`ServiceInfo` is still a 19-field god object** — the identity/lifecycle/health/graph split remains the highest-value refactor, deferred to the breaking batch.
7. **`dedupGraphEdges`** is still a local duplicate of go-output's `DedupEdges` — filing the upstream PR removes it.
8. **String-typed identifiers** — `ContainerID`/`ScopeID`/`ServiceName` as bare `string` lets impossible states compile. Named types would catch swaps at compile time.

### Documentation
9. **Reconcile the versioning story** — what is the *actual* next breaking version? Update TODO_LIST + create ROADMAP.md.
10. **Sync AGENTS.md** coverage claim (95%) with reality (94% gate, 94.6% actual) — or, better, fix the number by restoring coverage.

---

## F. Top #25 Things to Get Done Next

Sorted by impact × value ÷ effort (highest first).

| # | Task | Impact | Effort | Notes |
|---|------|--------|--------|-------|
| 1 | **Fix the 2 `unparam` lint issues** (`mkInvAfter`/`mkInvBefore`, `replay_test.go:79,86`) | HIGH | 10m | Drop the always-`"c"` `containerID` param or vary callers. Unblocks CI Lint. |
| 2 | **Commit the go-output v0.17.2 bump** | HIGH | 5m | Working-tree is tidy-consistent; fixes CI **go mod tidy** job. |
| 3 | **Restore `CODE_OF_CONDUCT.md`** (commit with `--no-verify`) | MED | 5m | Currently missing/untracked. |
| 4 | **Re-raise coverage to ≥95% and restore the gate to 95%** | HIGH | 2h | Write tests on tree/table paths instead of keeping the lowered 94% bar. |
| 5 | **Sync `AGENTS.md` coverage claim** (95% vs real 94% gate) | MED | 5m | Doc drift; do alongside #4. |
| 6 | **Resolve the BuildFlow pre-commit hook** | HIGH | 30m | Stop go.sum stripping + CoC deletion; or switch to `scripts/hooks`. |
| 7 | **Decide & document the versioning strategy** | HIGH | 15m | Next breaking = v0.4.0? v1.0? Affects the typed-ID batch. |
| 8 | **Fix `TODO_LIST.md` "v0.3.0" breaking-change mislabel** | MED | 5m | v0.3.0 shipped non-breaking; retarget the breaking batch. |
| 9 | **Create `ROADMAP.md`** | MED | 20m | Long-term direction is scattered; give it a home. |
| 10 | **Typed identifiers batch** (`ContainerID`/`ScopeID`/`ServiceName`) | HIGH | 4h | Breaking; 65+ compile sites; zero existing bugs — do once. |
| 11 | **`ServiceInfo` split** (identity/lifecycle/health/graph) | HIGH | 6h | Breaking; pair with #10. |
| 12 | **File upstream PR: `DedupEdges` for go-output/d2** | MED | 30m | Removes the local `dedupGraphEdges`. |
| 13 | **File upstream PR: graph-attribute setter for go-output** | MED | 1h | Unblocks #14. |
| 14 | **Restore DOT dark background + edge colors** | LOW | 1h | Blocked on #13. |
| 15 | **CSV/TSV demo in `example/exportReports()`** | LOW | 10m | Closes the example gap. |
| 16 | **Property-based testing** (`rapid`/`gopter`) for Diff/Filter/Migrate | MED | 2h | Needs a depguard exception. |
| 17 | **Update escape/enum/envdetect when go-output tags v0.17.2** | LOW | 5m | No tag exists upstream yet. |
| 18 | **Bump GitHub Actions off Node.js 20** | LOW | 10m | `checkout@v4`/`setup-go@v5`; deprecation warning. |
| 19 | **Add `govulncheck` to the pre-commit hook** | LOW | 15m | Currently CI-only. |
| 20 | **Bench for `buildDiagramNodes`/`buildDiagramEdges`** | LOW | 10m | Hot-path coverage. |
| 21 | **Collapse README diagram sections into one** | LOW | 10m | Less scrolling; style preference. |
| 22 | **D2 per-type node coloring** | LOW | 30m | Color nodes by provider type. |
| 23 | **`auditlog graph` CLI subcommand** | LOW | 1h | Standalone diagram gen from JSON. |
| 24 | **Streaming HTML export** via `go-output/markup` | LOW | 30m | Memory for huge graphs. |
| 25 | **Multi-module repository split** | LOW | 4h | Revisit at 5+ packages (currently 1). |

---

## G. Top #1 Question I Cannot Figure Out Myself

> **What is the actual versioning strategy, and which release carries the breaking changes?**
>
> `TODO_LIST.md` defers the typed-identifier + `ServiceInfo`-split work to **"v0.3.0"** — but **v0.3.0 just shipped as a non-breaking** tree/table release. So the documented next-breaking target is now contradicted by reality. Meanwhile the project is ALPHA, the last three releases (v0.1.0 → v0.2.0 → v0.3.0) were all non-breaking, and `STABILITY.md` promises 0.x stability for the "stable" API surface.
>
> This is a **product/stability decision I cannot make for you**: do the breaking changes go into **v0.4.0**, do you hold them until **v1.0.0** to signal stability, or do you do them now while still pre-1.0? The answer determines *when* to spend the 65+-compile-site refactor and *what* to put in `ROADMAP.md`. Everything in the Top-25 from #7 onward depends on it.

---

_Snapshot: branch `master` · HEAD `fc88b76` (1 commit ahead of `v0.3.0`) · working tree has uncommitted go-output v0.17.2 bump · latest CI run `27962578226` RED (Lint + go mod tidy)_
_Generated 2026-06-22 23:27 CEST_
