# Status Report: 2026-07-22 11:26 — Landing Page Redesign + Self-Review

## Session Scope

Redesign the landing page to move screenshots higher and use horizontal space better. Then brutally self-review the work.

---

## a) FULLY DONE

1. **Sections.ts inconsistency fixed** — Changed `"9+ export formats including HTML"` to `"16+ export formats including HTML"` in the comparison card data (`website/src/data/sections.ts:41`). This was a live inconsistency where the feature grid said "16+" but the comparison card below it said "9+".

2. **Screenshots moved above FeatureGrid** — `ShowcaseSection` now renders immediately after the Hero, before `FeatureGrid`. Users see the product visually before reading feature text. Changed in `website/src/pages/index.astro`.

3. **ShowcaseSection redesigned to 2x2 grid** — Replaced the old "1 hero + 3 thumbnails" hierarchy with a full-width (`max-w-6xl`) 2x2 grid where all 4 screenshots are equal-weight peers. Each card has browser chrome (3 dots + tab name), hover zoom, and a "Click to enlarge" affordance. First 2 images load eagerly, last 2 lazily.

4. **DemoSection redesigned to 2-column layout** — Replaced the narrow `max-w-3xl` single-column with `lg:grid-cols-2`. Left column: terminal mockup with realistic example output (Services: 20, Events: 146, Scopes: 4, Build: 45.2ms, HTML: audit-report.html). Right column: description, checklist, and source link.

5. **Website build verified** — `nix run .#build` succeeded. 13 pages built in 2.36s. HTML output confirmed: correct section ordering, grid classes present, "16+" text in comparison card.

---

## b) PARTIALLY DONE

1. **Changes are committed but NOT pushed** — A parallel process (likely BuildFlow's auto-commit) committed all session work with garbage AI-generated commit messages (`docs(website): update sections data configuration`, etc.). **7 commits are ahead of origin/master.** The live site at `do-auditlog.lars.software` has NONE of these changes. Nothing will be live until someone pushes.

2. **website/flake.lock is in a broken git state** — Commit `02b8afb` added `website/flake.lock` (auto-generated by `nix run .#build`). Then the deletion was staged, then the file was recreated by a subsequent nix build. Current state: HEAD has the file, index stages its deletion, worktree has it. Needs cleanup: either commit the deletion + re-add properly, or just restore to HEAD state.

3. **Prior session status report uncommitted** — `docs/status/2026-07-22_10-43_firebase-deploy-fix-and-live-site-verification.md` was mentioned as uncommitted in the handoff. It may have been committed by the parallel auto-commit process (commit `c94b351` appears to match).

---

## c) NOT STARTED

1. **Push to origin** — 7 unpushed commits. Live site unchanged.
2. **GitHub Releases** — v0.1.0 through v0.6.0 all missing. Releases page shows v0.0.4 as "Latest".
3. **Dependabot PR #1** — Still open, unmerged.
4. **Footer timestamp fix** — `html.templ` footer uses `new Date().toLocaleString()` (viewer's local time) instead of `report.exported_at` (report generation time). Misleading. Known from prior session, not addressed.
5. **OG image generation** — No social sharing image. Twitter/Slack/Discord link previews have no visual.
6. **Screenshot optimization** — All 4 screenshots are JPEGs (100-150KB each). Could be WebP for 30-50% size reduction.
7. **GitHub Actions SHA pinning** — Workflows use `@v4` tags instead of SHA hashes. Security best practice not met.

---

## d) TOTALLY FUCKED UP

1. **Auto-commit messages are garbage** — A parallel process committed work with hallucinated AI commit messages full of bullet-point padding that has zero relationship to the actual diff. Examples:
   - `"Update section visibility and display conditions based on user roles or permissions"` — there are no user roles or permissions in this project.
   - `"Add proper TypeScript types where applicable for improved developer experience"` — no TypeScript types were added.
   - `"Ensure proper TypeScript typing for section data consistency"` — a 1-character change ("9" to "16").
   These messages are noise in git history and should have been intercepted.

2. **website/flake.lock should not have been committed** — `flake.lock` for the website subflake was auto-generated by `nix run .#build` and then committed by the parallel process. The root project's `flake.lock` is tracked, but the website has its own flake. Whether `website/flake.lock` should be tracked or gitignored was never decided. Now it's in a broken half-deleted state.

3. **Timeline screenshot aspect ratio mismatch** — `html-timeline.jpg` is 1400x1100 while the other 3 screenshots are 1400x1300. In the 2x2 grid this creates visual inconsistency — the timeline card is shorter than its neighbors.

4. **"Click to enlarge" not touch-discoverable** — The affordance uses `opacity-0 group-hover:opacity-100` which only works on hover. On mobile/touch devices it's invisible. Users have no indication the screenshots are clickable links.

---

## e) WHAT WE SHOULD IMPROVE

### Immediate (broken or live-affecting)

1. **Push the 7 unpushed commits** — The redesign is invisible to users until pushed.
2. **Clean up flake.lock git state** — Restore or properly commit, not this half-staged mess.
3. **Fix timeline screenshot dimensions** — Regenerate at 1400x1300 to match siblings, or accept the mismatch.

### Design polish

4. **Add lightbox/gallery component** — Opening a raw JPG in a new tab is jarring. A proper lightbox with prev/next navigation would showcase the visualization professionally.
5. **Make "Click to enlarge" always visible on touch** — Use `@media (hover: none)` to show it permanently on touch devices, or add a caption bar below each screenshot.
6. **Equalize screenshot heights** — Either regenerate timeline at matching aspect ratio, or use CSS `object-fit: cover` with fixed heights in the grid.
7. **Add scroll-triggered fade-in animation** — The site already has `data-animate` and `animations.js`. The showcase grid should stagger-animate on scroll for visual impact.
8. **Add screenshot captions/annotations** — Each screenshot shows a complex UI. A 1-line caption under each ("20 services with dependency tracking, status badges, and build durations") would help users understand what they're looking at.

### Architecture

9. **DemoSection terminal output is hardcoded** — The mock terminal shows "Services: 20, Events: 146, Scopes: 4, Build: 45.2ms". If the example changes, these numbers silently drift. Should either be generated from the actual example or explicitly marked as illustrative.
10. **No automated visual regression testing** — The HTML report had a JS syntax error that passed all tests because the golden file check is byte-for-byte, not execution-based. The website redesign was verified by HTML grep, not visual diff.
11. **Screenshot generation is manual** — The screenshots in `website/public/images/` are generated via a manual Chromium + ImageMagick pipeline. Should be automated in CI or a Nix derivation.

### Content

12. **README "Sales page" quality** — The README was rewritten in a prior session but may still not do justice to the visual nature of the tool. The screenshots should be embedded in the README too, not just the website.
13. **No "When NOT to use this" section** — Honest software pages list contraindications. This plugin adds ~1.7us overhead; that's fine for most apps but matters for hot paths.
14. **No video/GIF demo** — Screenshots are static. The interactive graph (pan/zoom/click-to-highlight) and the waveform are dynamic features that static images can't convey.

---

## f) Up to 50 Things to Get Done Next

### Critical (live site broken/stale)

1. Push 7 unpushed commits to origin/master
2. Clean up `website/flake.lock` git state (staged deletion + worktree recreation)
3. Verify live site after push (CI/CD deploy triggers automatically)
4. Merge Dependabot PR #1 (`gh pr merge 1 --squash`)

### GitHub Releases

5. Create GitHub Release v0.6.0 with changelog notes
6. Create GitHub Release v0.5.0 with changelog notes
7. Create GitHub Release v0.4.0 with changelog notes
8. Create GitHub Release v0.3.0 with changelog notes
9. Create GitHub Release v0.2.0 with changelog notes
10. Create GitHub Release v0.1.0 with changelog notes

### Screenshot improvements

11. Regenerate `html-timeline.jpg` at 1400x1300 to match other screenshots
12. Convert all screenshots to WebP for smaller file sizes
13. Add screenshot generation script to `scripts/` for reproducibility
14. Embed screenshots in README.md (not just website)
15. Create a short GIF/video of the interactive graph (pan/zoom/click)

### Website design polish

16. Add staggered scroll animation to showcase grid cards
17. Make "Click to enlarge" visible on touch devices (`@media (hover: none)`)
18. Add 1-line caption under each screenshot explaining what the tab shows
19. Add a lightbox/gallery component for screenshot viewing (prev/next navigation)
20. Add OG image for social sharing (auto-generated from hero screenshot)
21. Add "When NOT to use this" section to landing page
22. Add testimonials or social proof section (if any GitHub stars/issues exist)
23. Add a "keyboard shortcuts" mini-section showing 1-5 tab navigation in the HTML report
24. Add favicon and theme color that match the warm amber aesthetic

### HTML report fixes

25. Fix footer timestamp in `html.templ` — use `report.exported_at` instead of `new Date().toLocaleString()`
26. Add headless browser test that executes the report's JavaScript and checks for errors
27. Add `frame-ancestors` CSP via HTTP header instead of `<meta>` tag (browsers ignore meta CSP for this directive)

### CI/CD improvements

28. Pin all GitHub Actions to SHA hashes in `website.yml`
29. Pin all GitHub Actions to SHA hashes in `ci.yml`
30. Add visual regression test to CI (screenshot diff on PR)
31. Add `website/flake.lock` to `.gitignore` or properly track it
32. Add deploy preview on PR (Firebase hosting preview channel)

### Code quality

33. Fix `doc.go` godoclint warning ("package has more than one godoc")
34. Run `go mod tidy` and verify no drift
35. Run full lint suite: `golangci-lint run`
36. Run full test suite with race detector: `go test -race ./...`

### Content and docs

37. Add "Who is this for?" section to README (backend devs using samber/do v2)
38. Add architecture diagram to README (or link to website)
39. Add performance benchmarks table to README
40. Update `FEATURES.md` with v0.6.0 feature inventory
41. Update `TODO_LIST.md` with current priorities
42. Write a blog post / Twitter thread about the HTML visualization feature
43. Add comparison section to README (vs manual logging, vs pprof, vs OpenTelemetry)

### DemoSection improvements

44. Make terminal mockup output dynamic or clearly mark as illustrative
45. Add "What to look for" annotations pointing to specific features in each screenshot
46. Add copy buttons for `go get` command (not just the demo command)

### Architecture

47. Add JSON Schema validation to the website's interactive playground (if one is added)
48. Consider adding a live playground where users can paste a report JSON and see the visualization
49. Add search engine optimization (structured data already present, but add breadcrumbs)
50. Add a changelog feed (RSS/Atom) for version updates

---

## g) Questions I Cannot Answer Myself

### 1. Should `website/flake.lock` be tracked in git or gitignored?

The root `flake.lock` is tracked. The website has its own separate flake (`website/flake.nix`). A parallel process auto-committed `website/flake.lock`, then staged its deletion. I don't know the project convention: should subflake lock files be tracked for reproducibility, or gitignored because they're build artifacts? This affects whether I restore the file or commit the deletion.

### 2. Should I push the 7 unpushed commits right now?

The commits include my redesign work AND changes from parallel processes (README rewrite, status reports) with garbage commit messages. Pushing makes them permanent in remote history. Do you want me to: (a) push as-is, (b) squash into clean commits first, or (c) wait for review? The live site is stale until something is pushed.

### 3. Are the garbage auto-commit messages acceptable, or should I rewrite git history?

Commits `ac48b84`, `4388c0e`, `02b8afb`, `6cd41a8` have AI-generated messages with fabricated bullet points ("user roles or permissions", "TypeScript typing") that don't match the actual diffs. Since they're unpushed, the history could be cleaned with an interactive rebase. But this is destructive and requires explicit approval.

---

## Session Summary

**What went well**: The redesign achieves both goals — screenshots are now the first thing users see after the hero, and the 2x2 grid + 2-column demo layout uses the full `max-w-6xl` width instead of wasting 35%+ of horizontal space. The build compiles clean and all structural changes verified in output HTML.

**What went wrong**: A parallel process auto-committed work with hallucinated commit messages, created a broken `flake.lock` git state, and nothing was pushed. The live site remains unchanged. The timeline screenshot has a mismatched aspect ratio that creates visual inconsistency in the grid.

**What I'd do differently**: Verify git state before declaring done. Push immediately after build verification. Intercept or disable auto-commit processes that generate garbage messages. Check image dimensions before placing them in a grid layout.
